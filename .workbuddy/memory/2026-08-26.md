# 2026-08-26

## 熟悉医学应试复习系统项目
- 通读 PROJECT_BRIEF.md、assets/app.js（25 维度定义 + 三模式逻辑）、index.html、data/*.js 全部 5 个科目。
- 架构确认：纯静态零构建、window.SUBJECTS 全局变量、25 维度（认知6/应用5/错误3/网络6含系统态/薄弱6）、localStorage 持久化 + file:// 降级内存、间隔重复 again/hard/good/easy。
- 内容覆盖：biochem 第1-2章共9点已填（ch3-16 仅骨架）；physiol/patho/internal/surgery 各2个种子点。

## 已发现的数据问题（待修）
1. ~~**biochem.js b1-2 笔误**~~ — 已修。c1_concept 改为「相邻氨基酸之间以肽键（酰胺键，-CO-NH-）连接。」。
2. **surgery.js 章节错位（仍待修）**：s2 章节名「第二章 麻醉」，但其唯一种子点标题却是「急性阑尾炎」（应属普通外科 s5）。属于种子点放错章节，需迁移或改章节名。

## 已完成任务（17:50）
- **② 场景 F 数据清理**：全量扫描 data/*.js（17 个已填点），维度键 25 个与 app.js DIMENSIONS 全部一致；除 b1-2 外无「更正/TODO/待改」类占位符。已清 b1-2 串台笔误。
- **③ 场景 C 进度导出/导入**：
  - index.html 顶栏加「导出进度」「导入进度」按钮 + 隐藏 `<input type=file id=importFile>`（手机端 topbar 仍可见，便于同步）。
  - styles.css 加 `.tools` 按钮样式（沿用 --brand/--line 变量）。
  - app.js 加 `exportProgress()`（导出 medreview_state_v1 为带 type 标记的 JSON）/ `importProgress()`（FileReader 读入、覆盖写回 localStorage、刷新 renderTree/renderMode/updateStat）；bindGlobal 中绑定。
  - 自验：`node --check` 全部 6 个 JS 通过；校验脚本确认维度一致、占位符清零、b1-2 修正生效。
  - 注意：本地 node 受 `NODE_OPTIONS=--use-system-ca` 阻塞，需用 `env -u NODE_OPTIONS` 运行。
  - 浏览器实测（导出→清空→导入→恢复）未自动跑，建议用户手动验 1 次。

## 18:09 补全生化第三章「酶」（场景 A）
- 在 biochem.js ch3 追加 8 个 25 维度知识点（原 ch3 为占位骨架，未动其他章节）：
  - b3-1 酶的概念与分子组成（单纯酶/结合酶/辅酶/辅基/核酶）
  - b3-2 酶的活性中心
  - b3-3 酶促反应的特点（高效/专一/不改平衡）
  - b3-4 米氏方程与 Km（含双倒数）
  - b3-5 竞争性/非竞争性/反竞争性抑制（Km/Vmax 变化对照 + 磺胺/丙二酸实例）
  - b3-6 酶原与酶原激活
  - b3-7 同工酶（LDH 亚基 M/H、LDH1 心 LDH5 肌）
  - b3-8 关键酶（限速酶）与酶的调节（别构 vs 共价修饰）
- 自验：node --check 通过；8 点各 25 维度键齐全无多余。
- 本地预览服务器仍在后台运行（http://127.0.0.1:8123/index.html），刷新即见新章节。

## 18:14 修复手机端"切换不自如"（侧栏点击不收起）
- 截图确认用户在窄屏（手机）视图里展开了侧栏。原始代码里点击侧栏的章节/知识点/科目后**侧栏不会自动收起**，导致右侧内容更新但被侧栏挡住 → 看起来"切不动"。
- 修改 `assets/app.js` 三处 onclick：selectSubject、章节标题点击、知识点点击 → 当 `window.innerWidth ≤ 768` 时移掉 `.sidebar.open`。
- 自验：`node --check assets/app.js` 通过。
- 其他仍可能影响体验的潜在问题（未修，等用户反馈）：renderBrowse/renderReview 全量 innerHTML 重渲染导致滚动位置丢失；编辑模式切到其他知识点前未自动保存表单；手机顶栏 5 个科目 Tab + 3 个模式 + 2 个工具 + 统计标签会换行挤掉。
